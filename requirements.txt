# core
numpy>=1.24
# visualization (Phase 6, 13)
matplotlib>=3.7
# testing (Phase 16)
pytest>=7.4
pytest-cov>=4.1
# benchmarking only (Phase 17) -- NOT used by the engine.
# install separately:  pip install torch
